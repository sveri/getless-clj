(ns de.sveri.getless.tags)
;  (:require [selmer.parser :as sp]))
;
;(sp/add-tag!
;  :foo
;  (fn [args context-map]
;    (clojure.pprint/pprint (:localize-fn context-map))
;    (str "foo " (first args))))
